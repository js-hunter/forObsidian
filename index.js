export default clas formValidation {
  static rules = getMapFromObj({...})

  constructor() {...}

  static getActionCfg(modal) {...}

  static getParams(handler) {...}

  static handleModalConfirm(instance, event) {...}

  static handleModalCancel(instance, event) {...}
}
